# Obtener el directorio de trabajo actual
mi_directorio <- getwd()
print(mi_directorio)
# Cambiar el directorio (descomentar y poner ruta real si se desea)
# setwd("C:/ruta/a/tu/directorio")
# Guardar todos los objetos del entorno de trabajo actual en un archivo .RData
# save.image(file = "mi_sesion_bis.RData")
a <- 1.7
1.7 -> b
c = 1.7
assign("d", 1.7)
# Mostrar valor en pantalla (print es necesario dentro de los scripts)
print(a)
a <- 1.7
1.7 -> b
c = 1.7
assign("d", 1.7)
# Mostrar valor en pantalla (print es necesario dentro de los scripts)
print(a)
# Obtener el directorio de trabajo actual
mi_directorio <- getwd()
print(mi_directorio)
# Cambiar el directorio (descomentar y poner ruta real si se desea)
# setwd("C:/ruta/a/tu/directorio")
# Guardar todos los objetos del entorno de trabajo actual en un archivo .RData
# save.image(file = "mi_sesion_bis.RData")
cambiarLetra <- function(cadena,letra, posicion){
# Asegurarse de que la posicion <= longitud cadena
if (posicion <= nchar(cadena)) {
substr(cadena, start = posicion, stop = posicion) <- letra
}
return(cadena)
}
nombres <- c("Juan", "bala", "cala")
vecReemp <- sapply(nombres, cambiarLetra,letra="o",posicion=2)
vecReemp
cambiarLetra <- function(cadena,letra, posicion){
# Asegurarse de que la posicion <= longitud cadena
if (posicion <= nchar(cadena)) {
# Esto básicamente coge una cadena y asigna x (en este caso 1 letra) a un rango de la cadena.
substr(cadena, start = posicion, stop = posicion) <- letra
}
return(cadena)
}
nombres <- c("Juan", "bala", "cala")
vecReemp <- sapply(nombres, cambiarLetra,letra="o",posicion=2)
vecReemp
cambiarLetra <- function(cadena,letra, posicion){
# Asegurarse de que la posicion <= longitud cadena
if (posicion <= nchar(cadena)) {
# Esto básicamente coge una cadena y asigna x (en este caso 1 letra) a un rango de la cadena.
substr(cadena, start = posicion, stop = posicion+1) <- letra
}
return(cadena)
}
nombres <- c("Juan", "bala", "cala")
vecReemp <- sapply(nombres, cambiarLetra,letra="o",posicion=2)
vecReemp
cambiarLetra <- function(cadena,letra, posicion){
# Asegurarse de que la posicion <= longitud cadena
if (posicion <= nchar(cadena)) {
# Esto básicamente coge una cadena y asigna x (en este caso 1 letra) a un rango de la cadena.
substr(cadena, start = posicion, stop = posicion) <- letra
}
return(cadena)
}
nombres <- c("Juan", "bala", "cala")
vecReemp <- sapply(nombres, cambiarLetra,letra="o",posicion=2)
vecReemp
setwd("C:/Users/alvar/Desktop/Nueva carpeta/IA Y BIG DATA/PIA/UT5")
Encuesta$Nivel_escolar <- factor(Encuesta$`Nivel escolar`,
levels = 0:5,
labels = c("Sin nivel/Primaria inc.", "Primaria comp./Sec inc.",
"Secundaria comp./Prep inc.", "Preparatoria comp./Lic inc.",
"Licenciatura comp./Pos inc.", "Posgrado terminado"))
Encuesta <- read.csv("Encuesta.csv",sep=",")
encuesta_clean <- Encuesta %>% filter(!is.na(Ingreso), !is.na('Tipo de vivienda'))
Encuesta <- read.csv("Encuesta.csv",sep=",")
encuesta_clean <- Encuesta %>% filter(!is.na(Ingreso), !is.na('Tipo de vivienda'))
encuesta_genero <- summarise(
group_by(encuesta_clean, Genero),
Ingreso_medio = mean(Ingreso, na.rm = TRUE)
)
library(dplyr)
library(tidyr)
library(readxl)
library(ggplot2)
Encuesta <- read.csv("Encuesta.csv",sep=",")
encuesta_clean <- Encuesta %>% filter(!is.na(Ingreso), !is.na('Tipo de vivienda'))
head(Encuesta)
encuesta_genero <- summarise(
group_by(encuesta_clean, Genero),
Ingreso_medio = mean(Ingreso, na.rm = TRUE)
)
encuesta_genero
ggplot(encuesta_genero, aes(x = Genero, y = Ingreso_medio)) + geom_bar(stat = "identity", colour='red', size= 33)
tabla_contingencia <- table(Encuesta$`Estado_civil`, Encuesta$Genero)
tabla_contingencia
Encuesta$Nivel_escolar <- factor(Encuesta$`Nivel escolar`,
levels = 0:5,
labels = c("Sin nivel/Primaria inc.", "Primaria comp./Sec inc.",
"Secundaria comp./Prep inc.", "Preparatoria comp./Lic inc.",
"Licenciatura comp./Pos inc.", "Posgrado terminado"))
Encuesta$Nivel_escolar <- factor(Encuesta$`Nivel escolar`,
levels = 0:5,
labels = c("Sin nivel/Primaria inc.", "Primaria comp./Sec inc.",
"Secundaria comp./Prep inc.", "Preparatoria comp./Lic inc.",
"Licenciatura comp./Pos inc.", "Posgrado terminado"))
Encuesta$Nivel_escolar <- factor(Encuesta$`Nivel escolar`,
levels = 0:5,
labels = c("Sin nivel/Primaria inc.", "Primaria comp./Sec inc.",
"Secundaria comp./Prep inc.", "Preparatoria comp./Lic inc.",
"Licenciatura comp./Pos inc.", "Posgrado terminado"))
Encuesta$Nivel_escolar <- factor(Encuesta$`Nivel_escolaridad`,
levels = 0:5,
labels = c("Sin nivel/Primaria inc.", "Primaria comp./Sec inc.",
"Secundaria comp./Prep inc.", "Preparatoria comp./Lic inc.",
"Licenciatura comp./Pos inc.", "Posgrado terminado"))
ggplot(Encuesta, aes(x = Nivel_escolaridad)) + geom_bar()
no_propia <- Encuesta[ Encuesta$`Tipo de vivienda` %in% c("Alquilada", "Prestada", "Otro"), ]
ggplot(no_propia, aes(x = Ingreso, y = Mensualidad)) + geom_point()
Encuesta <- read.csv("Encuesta.csv",sep=",")
encuesta_clean <- Encuesta %>% filter(!is.na(Ingreso), !is.na('Tipo_vivienda'))
head(Encuesta)
no_propia <- Encuesta[ Encuesta$`Tipo_vivienda` %in% c("Alquilada", "Prestada", "Otro"), ]
ggplot(no_propia, aes(x = Ingreso, y = Mensualidad)) + geom_point()
no_propia <- Encuesta[ Encuesta$`Tipo_vivienda` %in% c("Alquilada", "Prestada", "Otro","Propia pagando"), ]
ggplot(no_propia, aes(x = Ingreso, y = Mensualidad)) + geom_point()
no_propia <- Encuesta[ Encuesta$`Tipo_vivienda` %in% c("Alquilada", "Prestada", "Otro"), ]
ggplot(no_propia, aes(x = Ingreso, y = Mensualidad)) + geom_point()
ingreso_region <- aggregate(Ingreso ~ Región, data = Encuesta, FUN = list(promedio = mean, mediana = median))
ingreso_region <- aggregate(Ingreso ~ Region, data = Encuesta, FUN = mean, na.rm = TRUE)
ingreso_region
mediana_region <- aggregate(Ingreso ~ Region, data = Encuesta, FUN = median, na.rm = TRUE)
mediana_region
ggplot(ingreso_region, aes(x = Region, y = Ingreso)) +
geom_bar(stat = "identity")
tabla_vivienda_zona <- table(Encuesta$Tipo_vivienda, Encuesta$Zona)
tabla_vivienda_zona
tabla_vivienda_zona <- table(Encuesta$Tipo_vivienda, Encuesta$Zona)
tabla_vivienda_zona
tabla_vivienda_zona_df <- as.data.frame(tabla_vivienda_zona) #dataframe para representar el grafico
ggplot(tabla_vivienda_zona_df, aes(x = Var1, y = Freq, fill = Var2)) +
geom_bar(stat = "identity", position = "dodge")
tabla_porcentajes <- prop.table(table(Encuesta$Region, Encuesta$Nivel_escolaridad), margin = 1) * 100
tabla_porcentajes_df <- as.data.frame(as.table(tabla_porcentajes))
tabla_porcentajes <- prop.table(table(Encuesta$Region, Encuesta$Nivel_escolaridad), margin = 1) * 100
tabla_porcentajes_df <- as.data.frame(as.table(tabla_porcentajes))
ggplot(tabla_porcentajes_df, aes(x = Var1, y = Freq, fill = Var2)) +
geom_bar(stat = "identity")
ggplot(Encuesta, aes(x = Ingreso, fill = Genero)) +
geom_density(alpha = 0.5)
tabla_estado_nivel <- table(Encuesta$Estado_civil, Encuesta$Nivel_escolaridad)
ggplot(as.data.frame(as.table(tabla_estado_nivel)), aes(x = Var1, y = Var2, fill = Freq)) +
geom_tile()
library(shiny)
ui <- fluidPage(
titlePanel("Ejemplo Shiny"),
sidebarLayout(
sidebarPanel(
selectInput(
"variable",
"Variable:",
choices = c("mpg", "hp", "wt", "qsec")
),
sliderInput(
"bins",
"Barras:",
min = 5,
max = 20,
value = 10
)
),
mainPanel(
plotOutput("histograma"),
tableOutput("tabla")
)
)
)
server <- function(input, output) {
output$histograma <- renderPlot({
hist(
mtcars[[input$variable]],
breaks = input$bins,
col = "lightblue",
border = "black",
main = paste("Histograma de", input$variable),
xlab = input$variable
)
})
output$tabla <- renderTable({
datos <- head(mtcars[, input$variable, drop = FALSE])
colnames(datos) <- input$variable
datos
}, rownames = TRUE)
}
shinyApp(ui, server)
